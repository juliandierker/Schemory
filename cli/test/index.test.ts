// Placeholder test for @schemory/cli
// This ensures `pnpm -r test` runs green from commit one

describe('cli', () => {
  it('should parse commands correctly', () => {
    expect(true).toBe(true);
  });
});
