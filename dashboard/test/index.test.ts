// Placeholder test for @schemory/dashboard
// This ensures `pnpm -r test` runs green from commit one

describe('dashboard', () => {
  it('should render the dashboard', () => {
    expect(true).toBe(true);
  });
});
