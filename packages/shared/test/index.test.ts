// Placeholder test for @schemory/shared
// This ensures `pnpm -r test` runs green from commit one

describe('shared types', () => {
  it('should export types correctly', () => {
    // This test verifies that the shared types can be imported
    // In a real test suite, we'd use TypeScript type assertions
    expect(true).toBe(true);
  });
});
