// Placeholder test for @schemory/server
// This ensures `pnpm -r test` runs green from commit one

describe('server', () => {
  it('should be a Fastify server', () => {
    expect(true).toBe(true);
  });
});
